services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_URL: ${API_URL:-https://live.whotraveling.com}
    restart: unless-stopped
    expose:
      - "80"
