export enum VerificationStatus {
    PENDING = 'PENDING',
    REJECTED = 'REJECTED',
    ACCEPTED = 'ACCEPTED',
    EXPIRED = 'EXPIRED',
    }
    export enum AccountStatus {
        CREATED = 'CREATED',
        DEACTIVATED = 'DEACTIVATED',
        ACTIVATED = 'ACTIVATED',
        SUSPENDED = 'SUSPENDED',
        }